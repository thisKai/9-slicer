{
  "name": "9-slicer",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-native-modules": "electron-rebuild",
    "build-elm": "elm make ./app/Main.elm --output ./app.js",
    "build-elm-docker": "docker-compose run elm",
    "start": "electron .",
    "dev": "npm run build-elm && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.4.5",
    "lwip": "0.0.9"
  },
  "devDependencies": {
    "chokidar": "^1.6.1",
    "electron-rebuild": "^1.5.7",
    "elm": "^0.18.0"
  }
}
